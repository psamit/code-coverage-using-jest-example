{
  "name": "code-coverage-using-jest-example",
  "version": "1.0.0",
  "description": "Code coverage using jest",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --config jest.config.json --coverage",
    "test:watch": "jest --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.5.1"
  }
}
